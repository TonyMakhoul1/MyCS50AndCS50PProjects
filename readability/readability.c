#include <cs50.h>
#include <stdio.h>

int count_letters(string Text);

int main(void)
{
    string text;
    text = get_string("Text: ");

    printf("Text: %s\n",text);

}

int count_letters(string text)
{
    for( int i = 0; i < )
}
